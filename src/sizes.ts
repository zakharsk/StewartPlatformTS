export const
    winHeight = window.innerHeight,
    winWidth = window.innerWidth,
    H = 150,
    L = 210,
    n = 5,
    l = L/n,
    R = (L/2) / Math.cos(Math.PI/6),
    r0 = R * Math.sin(Math.PI/6),
    h = l * Math.sin(Math.PI/3)
